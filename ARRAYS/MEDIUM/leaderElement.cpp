#include<bits/stdc++.h>
using namespace std;

// Given an array of integers of size N, find all leader elements in the array.

// An element is called a leader if it is greater than or equal to all the elements to its right side.
// The rightmost element of the array is always a leader.

// Input:
// A = [16, 17, 4, 3, 5, 2]

// Output:
// 17 5 2

class Leader{
    public:
    
   




     
};
int main() {
    
    return 0;

}